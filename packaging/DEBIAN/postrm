#!/bin/bash
# On full purge (apt purge), remove generated files not tracked by dpkg.
if [ "$1" = "purge" ]; then
    rm -rf /opt/screen-share-tab/.venv
    rm -f  /opt/screen-share-tab/mpegts.min.js
fi
exit 0
